import { styled } from 'styled-components'

export const Container = styled.div`
  .container {
    width: 100%;
    max-width: 80rem;
    margin-right: auto;
    margin-left: auto;
    padding: 0 5rem;
  }
`
